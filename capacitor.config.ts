import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dadm.ionic.Luis',
  appName: 'Luis',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
